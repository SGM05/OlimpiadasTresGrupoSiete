class Carretera {
    constructor(x, y) {
        this.x = x;
        this.y = y;

    }

    Show() {
        
        fill(255);
        noStroke();
        rect(this.x, this.y, 5, 50, );
    }

      
    
}